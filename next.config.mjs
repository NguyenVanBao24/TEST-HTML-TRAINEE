/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["th.bing.com", "img.freepik.com", "www.slideserve.com", "cdn.elearningindustry.com"],
  },
};

export default nextConfig;
